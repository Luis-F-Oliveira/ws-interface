import { AxiosInstance } from "axios"

interface IServices {
    success: boolean
    message?: string
}

export function CommandsService() {
    function index(

    ): Promise<IServices> {
        return new Promise((resolve, reject) => {

        })
    }
    return {}
}