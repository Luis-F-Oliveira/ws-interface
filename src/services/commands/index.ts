import { AxiosInstance } from "axios"

interface IServices {
    success: boolean
    message?: string
    data?: 
}

export function CommandsService() {
    function index(

    ): Promise<> {
        return new Promise((resolve, reject) => {

        })
    }
    return {}
}